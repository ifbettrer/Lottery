# 抽奖系统技术开发规格说明书

## 1. 项目概述

### 1.1 项目目标

开发一个用于现场或活动场景的抽奖程序。系统由后端和前端组成：

- 后端负责参与者登记、号码分配、数据持久化、重复登记校验、抽奖逻辑和结果记录。
- 前端提供两个页面：
  - 扫描二维码进入的取号页面。
  - 抽奖操作页面，点击抽奖按钮后展示抽奖结果。

### 1.2 解决的问题

系统需要解决活动抽奖中的以下问题：

- 参与者可通过扫码自助登记姓名并取号。
- 同一姓名不可重复取号，重复登记时必须明确提示“不可重复取号”。
- 抽奖过程由系统随机选择已成功取号的参与者。
- 支持约 100 人在短时间内并发访问和登记，避免重复号码、脏数据、重复中奖等一致性问题。
- 抽奖结果需要清晰展示，并可由后端持久化保存，便于后续追溯。
- 中奖记录需要支持导出，便于活动结束后留档。
- 系统需要支持重置活动数据或重新开始抽奖，便于新一轮活动复用。

### 1.3 使用场景

- 活动现场参与者使用手机扫描二维码，进入取号页面。
- 参与者填写个人姓名并提交登记。
- 系统校验姓名是否已登记：
  - 未登记则分配唯一号码并保存。
  - 已登记则拒绝保存并提示“不可重复取号”。
- 主持人或管理员打开抽奖页面，点击抽奖按钮；抽奖页面不要求登录、访问口令或鉴权。
- 系统从符合条件的号码池中随机抽取中奖者，并在页面展示中奖号码和姓名。
- 活动管理人员可导出中奖记录。
- 活动管理人员可执行重置操作，清空活动数据并重新开始。
- 扫码二维码的普通参与者只能进入取号页面，不应通过二维码直接进入抽奖页面或管理功能页面。

## 2. 输入定义

### 2.1 参与者登记输入

| 参数名 | 类型 | 必填 | 取值范围 / 格式 | 边界值 | 非法输入 |
| --- | --- | --- | --- | --- | --- |
| name | string | 是 | 参与者姓名，去除首尾空白后长度 1-50 个字符 | 最短 1 个字符，最长 50 个字符 | 空字符串、纯空白、超过 50 个字符、非字符串类型 |

### 2.2 抽奖输入

| 参数名 | 类型 | 必填 | 取值范围 / 格式 | 边界值 | 非法输入 |
| --- | --- | --- | --- | --- | --- |
| count | integer | 否 | 单次抽取人数，默认 1；本期唯一允许值为 1 | 1 | 小于 1、非整数、大于 1 |
| exclude_winners | boolean | 否 | 是否排除已中奖人员，默认 true | true；false | 非布尔值 |

说明：

- 本期固定实现单人抽奖，即 `count` 必须为 1 或省略。
- 系统允许多次触发抽奖按钮，每次触发视为一轮独立抽奖。
- 已中奖人员永久排除，不得在后续任何轮次中再次中奖。
- `exclude_winners` 固定为 true；如前端或调用方传入 false，后端必须忽略或拒绝，不能允许已中奖人员再次进入候选池。
- 抽奖页面的按钮点击行为等价于向后端发起一次抽奖请求。

### 2.3 中奖记录导出输入

| 参数名 | 类型 | 必填 | 取值范围 / 格式 | 边界值 | 非法输入 |
| --- | --- | --- | --- | --- | --- |
| format | string | 否 | 导出格式，默认 csv；允许值 csv | csv | 不支持的格式值 |

说明：

- 本期至少支持 CSV 格式导出中奖记录。
- 导出内容必须来自后端已持久化的中奖记录。

### 2.4 重置活动输入

| 参数名 | 类型 | 必填 | 取值范围 / 格式 | 边界值 | 非法输入 |
| --- | --- | --- | --- | --- | --- |
| confirm | boolean | 是 | 必须为 true | true | false、缺失、非布尔值 |
| reset_scope | string | 否 | 重置范围，默认 all；允许值 all | all | 不支持的范围值 |

说明：

- `reset_scope=all` 表示清空参与者记录、中奖记录，并将号码递增序列重置为初始状态。
- 重置为高风险操作，前端必须要求二次确认，后端必须要求明确确认参数。

### 2.5 页面访问输入

| 页面 | 输入来源 | 参数 | 要求 |
| --- | --- | --- | --- |
| 取号页面 | 二维码 URL | 无强制业务参数 | 页面必须可被移动端浏览器访问；二维码只能指向取号页面 |
| 抽奖页面 | 浏览器 URL | 无强制业务参数 | 页面必须展示抽奖按钮、抽奖结果区域、中奖记录导出入口和重置活动入口；不要求登录或访问口令 |

### 2.6 数据存储字段

参与者记录至少包含：

| 字段名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | integer/string | 是 | 系统内部唯一标识 |
| name | string | 是 | 参与者姓名，需唯一 |
| number | integer | 是 | 系统分配的唯一递增号码，从 1 开始，每成功登记 1 人递增 1 |
| created_at | datetime | 是 | 登记时间 |

抽奖记录至少包含：

| 字段名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | integer/string | 是 | 抽奖记录唯一标识 |
| participant_id | integer/string | 是 | 中奖参与者标识 |
| number | integer | 是 | 中奖号码 |
| name | string | 是 | 中奖者姓名 |
| round | integer | 是 | 抽奖轮次，按成功抽奖次数从 1 递增 |
| created_at | datetime | 是 | 中奖时间 |

## 3. 输出定义

### 3.1 通用返回格式

后端接口返回应使用统一结构：

| 字段名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| success | boolean | 是 | 请求是否成功 |
| data | object/null | 是 | 成功时返回业务数据，失败时为 null 或补充上下文 |
| error | object/null | 是 | 成功时为 null，失败时返回错误信息 |

错误对象格式：

| 字段名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | string | 是 | 机器可读错误码 |
| message | string | 是 | 用户可读错误信息 |

### 3.2 登记成功输出

成功登记时返回：

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| id | integer/string | 参与者唯一标识 |
| name | string | 登记姓名 |
| number | integer | 分配号码，递增数字，从 1 开始 |
| created_at | datetime/string | 登记时间 |

页面展示要求：

- 展示登记成功状态。
- 展示参与者姓名和取到的号码。

### 3.3 登记失败输出

| 场景 | 错误码 | 错误信息 |
| --- | --- | --- |
| 姓名为空或格式不合法 | INVALID_NAME | 姓名不能为空或格式不正确 |
| 姓名已存在 | DUPLICATE_NAME | 不可重复取号 |
| 系统保存失败 | STORAGE_ERROR | 系统繁忙，请稍后重试 |

重复姓名场景必须向用户展示“不可重复取号”。

### 3.4 抽奖成功输出

单人抽奖成功时返回：

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| winner.id | integer/string | 中奖参与者唯一标识 |
| winner.name | string | 中奖者姓名 |
| winner.number | integer | 中奖号码 |
| draw.round | integer | 抽奖轮次 |
| draw.created_at | datetime/string | 抽奖时间 |

页面展示要求：

- 抽奖完成后展示中奖号码。
- 抽奖完成后展示中奖者姓名。
- 抽奖完成后展示当前抽奖轮次。
- 抽奖按钮允许多次触发，但请求进行中应避免同一点击产生重复请求。

### 3.5 抽奖失败输出

| 场景 | 错误码 | 错误信息 |
| --- | --- | --- |
| 没有参与者 | NO_PARTICIPANTS | 暂无可抽奖人员 |
| 所有参与者均已中奖且不允许重复中奖 | NO_AVAILABLE_PARTICIPANTS | 暂无可抽奖人员 |
| 抽奖参数非法 | INVALID_DRAW_REQUEST | 抽奖请求不合法 |
| 系统保存失败 | STORAGE_ERROR | 系统繁忙，请稍后重试 |

### 3.6 中奖记录导出输出

导出成功时返回 CSV 文件或文件下载响应，内容至少包含以下列：

| 列名 | 类型 | 说明 |
| --- | --- | --- |
| round | integer | 抽奖轮次 |
| number | integer | 中奖号码 |
| name | string | 中奖者姓名 |
| created_at | datetime/string | 中奖时间 |

导出失败时返回：

| 场景 | 错误码 | 错误信息 |
| --- | --- | --- |
| 导出格式不支持 | INVALID_EXPORT_FORMAT | 导出格式不支持 |
| 读取中奖记录失败 | STORAGE_ERROR | 系统繁忙，请稍后重试 |

### 3.7 重置活动输出

重置成功时返回：

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| reset | boolean | 固定为 true |
| reset_scope | string | 重置范围，本期为 all |
| reset_at | datetime/string | 重置完成时间 |

重置失败时返回：

| 场景 | 错误码 | 错误信息 |
| --- | --- | --- |
| 未确认重置 | RESET_CONFIRM_REQUIRED | 请确认后再重置 |
| 重置范围不支持 | INVALID_RESET_SCOPE | 重置请求不合法 |
| 重置保存失败 | STORAGE_ERROR | 系统繁忙，请稍后重试 |

## 4. 功能需求清单

1. 系统必须提供取号页面，用户通过二维码链接访问。
2. 取号页面必须提供姓名输入框和提交入口。
3. 用户提交姓名后，系统必须去除姓名首尾空白再进行校验和保存。
4. 系统必须拒绝空姓名、纯空白姓名和超过长度限制的姓名。
5. 系统必须保证姓名唯一，同一姓名只能成功取号一次。
6. 当用户使用已登记姓名再次取号时，系统必须提示“不可重复取号”。
7. 系统必须为每个成功登记的参与者分配唯一递增数字号码，首个号码为 1，之后每成功登记 1 人递增 1。
8. 系统必须持久化保存参与者姓名、号码和登记时间。
9. 系统必须提供抽奖页面，抽奖页面不要求管理员登录或访问口令。
10. 抽奖页面必须提供抽奖按钮。
11. 用户点击抽奖按钮后，前端必须向后端发起抽奖请求。
12. 后端必须从已成功登记的参与者中随机抽取中奖者。
13. 抽奖成功后，前端必须展示中奖者姓名和中奖号码。
14. 抽奖成功后，前端必须展示当前抽奖轮次。
15. 系统必须持久化保存中奖记录。
16. 系统必须允许多次触发抽奖按钮，每次成功触发产生一轮抽奖，每轮中奖人数固定为 1。
17. 已中奖人员必须永久排除，不得在后续任何轮次中再次中奖。
18. 当暂无可抽奖人员时，系统必须返回明确错误信息，不得返回空白或崩溃。
19. 前端在单次登记或单次抽奖请求处理中必须防止同一操作重复提交。
20. 后端必须在并发登记场景下保证姓名和号码唯一。
21. 后端必须在并发抽奖场景下保证不会产生重复中奖。
22. 系统必须提供清晰的成功、失败和加载状态反馈。
23. 二维码必须只指向取号页面，扫码取号用户不应通过二维码进入抽奖页面、导出功能或重置功能。
24. 系统必须提供中奖记录导出功能，至少支持 CSV。
25. 导出的中奖记录必须包含抽奖轮次、中奖号码、中奖者姓名和中奖时间。
26. 系统必须提供重置活动数据或重新开始抽奖的管理功能。
27. 重置活动数据时必须清空参与者记录和中奖记录，并将号码递增序列恢复到初始状态。
28. 重置活动必须要求明确确认，避免误操作。

## 5. 非功能需求

### 5.1 性能

- 系统应支持约 100 名用户在短时间内并发访问取号页面并提交登记。
- 单次登记请求在正常环境下应在 1 秒内完成。
- 单次抽奖请求在正常环境下应在 1 秒内完成。
- 前端页面首次加载应尽量轻量，适配移动端扫码访问。

### 5.2 精度与随机性

- 抽奖候选池必须只包含成功登记且符合抽奖条件的参与者。
- 抽奖候选池必须永久排除所有已中奖参与者。
- 每轮抽奖中奖人数固定为 1。
- 随机抽取应避免固定顺序、时间顺序或号码顺序造成偏差。
- 抽奖结果必须与后端持久化记录一致。

### 5.3 兼容性

- 取号页面必须兼容主流移动端浏览器。
- 抽奖页面必须兼容主流桌面浏览器。
- 页面应支持常见屏幕尺寸，移动端不应出现关键内容不可见或无法点击。

### 5.4 数据一致性

- 姓名唯一性必须由后端保证，不能只依赖前端校验。
- 号码唯一性和递增顺序必须由后端保证。
- 并发写入时必须使用事务、唯一约束、锁或等效机制保证一致性。
- 抽奖记录保存失败时，不得向前端返回已成功中奖的结果。
- 重置操作必须是原子操作；不得出现只清空参与者但未清空中奖记录，或只重置号码序列但未清空数据的中间状态。

### 5.5 安全与约束

- 用户输入必须进行基础清理，避免脚本注入内容被直接渲染执行。
- 前端展示姓名时必须进行安全转义或使用安全渲染机制。
- 系统不得在错误信息中泄露数据库连接、堆栈、文件路径等内部细节。
- 抽奖页面、导出功能和重置功能本阶段不要求登录鉴权或访问口令。
- 二维码内容必须配置为取号页面地址，不能配置为抽奖页面、导出接口或重置接口地址。
- 由于抽奖页面和管理功能不做登录鉴权，部署时应通过二维码分发、页面路径保密或现场网络控制降低误访问风险。

## 6. 边界条件 & 异常场景清单

### 6.1 登记边界与异常

- 姓名为空。
- 姓名为纯空白。
- 姓名前后包含空格。
- 姓名长度正好为 1 个字符。
- 姓名长度正好为 50 个字符。
- 姓名长度超过 50 个字符。
- 姓名包含中文、英文、数字或常见符号。
- 姓名包含 HTML 或脚本片段。
- 两个用户同时提交完全相同姓名。
- 两个用户提交只在首尾空白上不同的姓名。
- 数据库或存储不可用。
- 登记成功后网络中断，用户重复点击提交。
- 页面刷新后再次提交同一姓名。

### 6.2 号码分配边界与异常

- 首位参与者登记。
- 第 100 位参与者登记。
- 并发登记时多个请求同时申请号码。
- 号码生成或保存失败。
- 已分配号码不得被其他参与者复用。
- 重置活动后，首位新参与者号码重新从 1 开始。
- 登记失败的请求不得消耗号码。

### 6.3 抽奖边界与异常

- 无任何参与者时点击抽奖。
- 仅有 1 名可抽参与者时点击抽奖。
- 所有参与者均已中奖。
- 抽奖请求参数 `count` 小于 1。
- 抽奖请求参数 `count` 大于 1。
- 抽奖请求参数 `exclude_winners=false`。
- 抽奖按钮被连续快速点击。
- 多个浏览器同时发起抽奖请求。
- 抽奖结果生成成功但中奖记录保存失败。
- 数据库或存储在抽奖过程中不可用。
- 多轮抽奖后，历史中奖者不得再次出现在候选池。
- 抽奖轮次必须按成功抽奖次数递增，失败抽奖不得消耗轮次。

### 6.4 导出与重置异常

- 无中奖记录时导出。
- 导出格式不是 csv。
- 导出过程中存储不可用。
- 重置请求未携带确认参数。
- 重置请求 `confirm=false`。
- 重置过程中存储不可用。
- 重置后立即登记。
- 重置后立即抽奖且还没有新参与者。

### 6.5 页面访问与前端展示异常

- 登记接口超时。
- 抽奖接口超时。
- 导出接口超时。
- 重置接口超时。
- 后端返回非预期格式。
- 用户在弱网环境下重复点击按钮。
- 移动端屏幕较窄时输入框、按钮、提示信息必须可见。
- 扫码二维码后必须进入取号页面，而不是抽奖页面或管理功能页面。

## 7. 验收测试用例列表

### 7.1 正向用例

| 编号 | 场景 | 输入 | 预期输出 |
| --- | --- | --- | --- |
| TC-P-001 | 首次登记成功 | name = "张三" | 返回 success=true，data.name="张三"，data.number 存在且唯一；页面展示取号成功 |
| TC-P-002 | 姓名前后空格自动清理 | name = "  李四  " | 返回 success=true，data.name="李四" |
| TC-P-003 | 多人登记成功 | 依次提交 "张三"、"李四"、"王五" | 三次均成功，三人的 number 互不相同 |
| TC-P-004 | 递增号码分配 | 依次提交 "张三"、"李四"、"王五" | 三次均成功，号码分别为 1、2、3 |
| TC-P-005 | 单人抽奖成功 | 已有至少 1 名参与者，点击抽奖 | 返回 success=true，展示中奖者姓名、号码和轮次 |
| TC-P-006 | 中奖记录保存 | 抽奖成功一次 | 后端存在对应中奖记录，记录中的姓名、号码和轮次与前端展示一致 |
| TC-P-007 | 移动端访问取号页 | 手机浏览器打开二维码链接 | 页面可正常输入姓名并提交 |
| TC-P-008 | 桌面端访问抽奖页 | 桌面浏览器打开抽奖页面 | 页面显示抽奖按钮、结果区域、导出入口和重置入口 |
| TC-P-009 | 多轮抽奖 | 已有 3 名参与者，连续成功点击抽奖 3 次 | 每次抽出 1 人，轮次为 1、2、3，三次中奖者互不相同 |
| TC-P-010 | 导出中奖记录 | 已有至少 1 条中奖记录，执行导出 | 返回 CSV，包含 round、number、name、phone、created_at 列 |
| TC-P-011 | 重置活动数据 | 已有参与者和中奖记录，confirm=true，reset_scope=all | 返回 success=true；参与者和中奖记录为空；下一位登记者号码为 1 |
| TC-P-012 | 二维码进入取号页 | 扫描活动二维码 | 浏览器打开取号页面，不进入抽奖页面、导出功能或重置功能 |

### 7.2 反向用例

| 编号 | 场景 | 输入 | 预期输出 |
| --- | --- | --- | --- |
| TC-N-001 | 空姓名登记 | name = "" | 返回 success=false，error.code="INVALID_NAME" |
| TC-N-002 | 纯空白姓名登记 | name = "   " | 返回 success=false，error.code="INVALID_NAME" |
| TC-N-003 | 重复姓名登记 | 先提交 name="张三" 成功，再提交 name="张三" | 第二次返回 success=false，error.code="DUPLICATE_NAME"，message="不可重复取号" |
| TC-N-004 | 只差首尾空格的重复姓名 | 先提交 name="张三" 成功，再提交 name=" 张三 " | 第二次返回 success=false，error.code="DUPLICATE_NAME"，message="不可重复取号" |
| TC-N-005 | 超长姓名 | name 为 51 个字符 | 返回 success=false，error.code="INVALID_NAME" |
| TC-N-006 | 无参与者抽奖 | 参与者表为空，点击抽奖 | 返回 success=false，error.code="NO_PARTICIPANTS" 或 "NO_AVAILABLE_PARTICIPANTS" |
| TC-N-007 | 所有人已中奖后继续抽奖 | 所有参与者均已有中奖记录 | 返回 success=false，error.code="NO_AVAILABLE_PARTICIPANTS" |
| TC-N-008 | 非法抽奖人数 | count=0 | 返回 success=false，error.code="INVALID_DRAW_REQUEST" |
| TC-N-009 | 抽奖人数不等于 1 | count=2 | 返回 success=false，error.code="INVALID_DRAW_REQUEST" |
| TC-N-010 | 尝试允许重复中奖 | exclude_winners=false | 后端不得让已中奖人员进入候选池；应忽略该参数或返回 INVALID_DRAW_REQUEST |
| TC-N-011 | 不支持的导出格式 | format="xlsx" | 返回 success=false，error.code="INVALID_EXPORT_FORMAT" |
| TC-N-012 | 未确认重置 | confirm=false 或缺失 | 返回 success=false，error.code="RESET_CONFIRM_REQUIRED" |
| TC-N-013 | 不支持的重置范围 | confirm=true，reset_scope="winners_only" | 返回 success=false，error.code="INVALID_RESET_SCOPE" |

### 7.3 边界用例

| 编号 | 场景 | 输入 | 预期输出 |
| --- | --- | --- | --- |
| TC-B-001 | 最短姓名 | name = "A" | 登记成功，号码唯一 |
| TC-B-002 | 最长合法姓名 | name 为 50 个字符 | 登记成功，号码唯一 |
| TC-B-003 | 第 100 人登记 | 已有 99 人成功登记，再提交第 100 个不同姓名 | 登记成功，系统响应正常 |
| TC-B-004 | 100 人近似并发登记 | 100 个不同姓名在短时间内同时提交 | 全部成功，姓名唯一，号码为 1-100 且无重复，无服务崩溃 |
| TC-B-005 | 并发重复登记 | 10 个请求同时提交 name="张三" | 仅 1 个成功，其余返回 DUPLICATE_NAME |
| TC-B-006 | 仅 1 名参与者抽奖 | 只有 1 名未中奖参与者，点击抽奖 | 该参与者中奖，结果保存成功 |
| TC-B-007 | 快速连续点击抽奖按钮 | 前端连续触发多次点击 | 每个成功请求最多产生 1 名中奖者；已中奖人员不会重复中奖；轮次连续递增 |
| TC-B-008 | 姓名包含脚本片段 | name = "<script>alert(1)</script>" | 系统不得执行脚本；如允许保存，展示时必须安全转义；如按规则拒绝，返回 INVALID_NAME |
| TC-B-009 | 无中奖记录导出 | 中奖记录为空，执行 CSV 导出 | 返回空数据 CSV 或仅表头 CSV，不报错 |
| TC-B-010 | 重置后重新登记 | 完成重置后提交 name="张三" | 登记成功，number=1 |
| TC-B-011 | 重置后立即抽奖 | 完成重置且无新参与者，点击抽奖 | 返回 success=false，error.code="NO_PARTICIPANTS" 或 "NO_AVAILABLE_PARTICIPANTS" |

## 8. 待确认事项

以下事项仍需人工审核时确认：

1. 姓名唯一性是否区分大小写、全角半角、繁简体。
2. 重置功能是否需要保留历史导出文件，或重置后完全不可追溯。
3. 导出文件的文件名格式是否需要包含活动名称或时间戳。
4. 是否需要导出参与者名单；当前仅要求导出中奖记录。
